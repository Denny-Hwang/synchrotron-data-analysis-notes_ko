"""eBERlight Explorer — Reusable UI components.

Each module exports one or more render_* functions that produce HTML
via st.markdown(unsafe_allow_html=True), styled per design_system.md (DS-001).
"""
